export default class ScheduleModel {
    constructor(img, title, pg, description, times, _id) {
      this.img = img;
      this.title = title;
      this.pg = pg;
      this.times = times;
      this._id = _id;
    }
  }
  